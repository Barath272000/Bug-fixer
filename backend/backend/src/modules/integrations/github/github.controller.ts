import{z}from'zod';import{listRepos,connectRepository}from'./github.service.js';export async function repos(request:import('express').Request,response:import('express').Response){try{const token=z.string().min(10).parse(request.header('x-github-token'));response.json(await listRepos(token));}catch(error){throw error;}}export async function connect(request:import('express').Request,response:import('express').Response){try{const token=z.string().min(10).parse(request.header('x-github-token'));const input=z.object({projectId:z.string().uuid(),owner:z.string().min(1),repo:z.string().min(1),branch:z.string().min(1).optional()}).parse(request.body);response.json(await connectRepository(request.user!.id,input.projectId,token,input.owner,input.repo,input.branch));}catch(error){throw error;}}
