import{prisma}from'../../config/database.js';export async function indexDocument(id:string,content:string){const document=await prisma.contextDocument.findUnique({where:{id}});if(!document)throw new Error('Context document not found');await prisma.contextChunk.deleteMany({where:{documentId:id}});const chunks=[];const size=1800;for(let i=0;i<content.length;i+=size)chunks.push({documentId:id,ordinal:Math.floor(i/size),content:content.slice(i,i+size)});if(chunks.length)await prisma.contextChunk.createMany({data:chunks});return prisma.contextDocument.update({where:{id},data:{contentText:content,indexedAt:new Date()}});}
