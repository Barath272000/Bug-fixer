import{Router}from'express';import{requireAuth}from'../../common/middleware/auth.middleware.js';import{asyncHandler}from'../../common/utils/async-handler.js';import{list,get,generate,validate,apply,revert}from'./fix.controller.js';export const fixRoutes=Router();fixRoutes.use(requireAuth);fixRoutes.get('/history',asyncHandler(list));fixRoutes.get('/:id',asyncHandler(get));fixRoutes.post('/generate',asyncHandler(generate));fixRoutes.post('/:id/validate',asyncHandler(validate));fixRoutes.post('/:id/apply',asyncHandler(apply));fixRoutes.post('/:id/revert',asyncHandler(revert));
