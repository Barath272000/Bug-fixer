import fs from'node:fs/promises';import path from'node:path';export async function detectLanguage(root:string){const files=await fs.readdir(root);if(files.includes('package.json'))return'JavaScript';if(files.includes('pyproject.toml')||files.includes('requirements.txt'))return'Python';if(files.includes('go.mod'))return'Go';if(files.includes('Cargo.toml'))return'Rust';if(files.includes('pom.xml')||files.includes('build.gradle'))return'Java';const counts=new Map<string,number>();const walk=async(dir:string)=>{for(const name of await fs.readdir(dir,{withFileTypes:true})){if(name.name.startsWith('.')||name.name==='node_modules')continue;const full=path.join(dir,name.name);if(name.isDirectory())await walk(full);else{const ext=path.extname(name.name).toLowerCase();const lang={'.ts':'TypeScript','.tsx':'TypeScript','.js':'JavaScript','.jsx':'JavaScript','.py':'Python','.go':'Go','.rs':'Rust','.java':'Java','.sql':'SQL','.css':'CSS','.html':'HTML'}[ext];if(lang)counts.set(lang,(counts.get(lang)??0)+1);}}};await walk(root);let best='Unknown';let bestCount=0;for(const [lang,count] of counts){if(count>bestCount){best=lang;bestCount=count;}}return best;}
