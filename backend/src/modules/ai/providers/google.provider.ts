import type{AIProvider,ChatRequest}from'./base.provider.js';import{env}from'../../../config/env.js';export class GoogleProvider implements AIProvider{async chat(request:ChatRequest){try{if(!request.apiKey)throw new Error('Google API key is not configured');const url=`${request.baseUrl??env.GOOGLE_BASE_URL}/models/${encodeURIComponent(request.model)}:generateContent?key=${encodeURIComponent(request.apiKey)}`;const response=await fetch(url,{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({systemInstruction:{parts:[{text:request.system}]},contents:[{role:'user',parts:[{text:request.user}]}],generationConfig:{temperature:request.temperature??0.1,maxOutputTokens:request.maxTokens??6000}})});if(!response.ok)throw new Error(`Google request failed with status ${response.status}`);const data=await response.json() as {candidates?:Array<{content?:{parts?:Array<{text?:string}>}}>};const text=data.candidates?.[0]?.content?.parts?.map((x)=>x.text??'').join('');if(!text)throw new Error('Google returned an empty response');return text;}catch(error){throw new Error(`Google provider error: ${error instanceof Error?error.message:String(error)}`);}}}
