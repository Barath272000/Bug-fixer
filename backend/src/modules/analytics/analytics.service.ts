import{prisma}from'../../config/database.js';import{AppError}from'../../common/errors/AppError.js';
export async function getAnalytics(userId:string,projectId:string){const project=await prisma.project.findFirst({where:{id:projectId,ownerId:userId}});if(!project)throw new AppError(404,'PROJECT_NOT_FOUND','Project was not found');const [bugs,fixes,tests]=await Promise.all([prisma.bug.findMany({where:{projectId},select:{status:true,severity:true,createdAt:true,updatedAt:true}}),prisma.fixProposal.findMany({where:{projectId},select:{status:true,confidence:true,createdAt:true,appliedAt:true}}),prisma.testRun.findMany({where:{projectId},select:{total:true,passed:true,failed:true,createdAt:true}})]);const totalTests=tests.reduce((s,t)=>s+t.total,0);const passedTests=tests.reduce((s,t)=>s+t.passed,0);const repaired=fixes.filter((f)=>f.status==='Applied').length;const mean=bugs.length?bugs.reduce((s,b)=>s+(b.updatedAt.getTime()-b.createdAt.getTime()),0)/bugs.length:0;return{mttrMinutes:Math.round(mean/60000),aiRepairedBugs:repaired,testPassRate:totalTests?Math.round((passedTests/totalTests)*100):0,bugsDetected:bugs.length,fixesGenerated:fixes.length,aiComputeCost:0,rootCauses:bugs.reduce<Record<string,number>>((acc,b)=>{const key=b.severity;acc[key]=(acc[key]??0)+1;return acc;},{}),timeline:{bugs:bugs.map((b)=>({date:b.createdAt.toISOString().slice(0,10),status:b.status})),fixes:fixes.map((f)=>({date:f.createdAt.toISOString().slice(0,10),status:f.status,confidence:f.confidence}))}};}
