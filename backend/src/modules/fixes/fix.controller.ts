import{z}from'zod';import{generateFix,listFixes,getFix,validateFix,applyFix,revertFix}from'./fix.service.js';const generateSchema=z.object({bugId:z.string().uuid(),provider:z.string().optional(),model:z.string().optional()});const validateSchema=z.object({command:z.string().min(1).max(1000).default('npm test')});
export async function list(request:import('express').Request,response:import('express').Response){try{response.json(await listFixes(request.user!.id));}catch(error){throw error;}}export async function get(request:import('express').Request,response:import('express').Response){try{response.json(await getFix(request.user!.id,z.string().uuid().parse(request.params.id)));}catch(error){throw error;}}export async function generate(request:import('express').Request,response:import('express').Response){try{const input=generateSchema.parse(request.body);response.status(202).json(await generateFix(request.user!.id,input.bugId,input.provider,input.model));}catch(error){throw error;}}export async function validate(request:import('express').Request,response:import('express').Response){try{response.json(await validateFix(request.user!.id,z.string().uuid().parse(request.params.id),validateSchema.parse(request.body).command));}catch(error){throw error;}}export async function apply(request:import('express').Request,response:import('express').Response){try{response.json(await applyFix(request.user!.id,z.string().uuid().parse(request.params.id)));}catch(error){throw error;}}export async function revert(request:import('express').Request,response:import('express').Response){try{response.json(await revertFix(request.user!.id,z.string().uuid().parse(request.params.id)));}catch(error){throw error;}}
